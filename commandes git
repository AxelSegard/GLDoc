git clone https://AxelSegard@github.com/AxelSegard/casse_bash.git

git init	 // Initialiser le depot // Pas faire de git init dans un sous dossier, sinon caca
git add       	 // Ajouter dans la future version
git status	 // Voir les modifications actuelles avant le commit
git commit -a	 // commiter dans le depot
git push   	 // Envoyer sur github.com
git remote set-url origin https://github.com/AxelSegard/GLDoc.git // Changer le dossier utiliser dans github
git remote add origin https://github.com/AxelSegard/GLDoc.git



git diff   	 // Voir les modifications détaillées 
git log		 // affiche tous les commits
git checkout master 	    //Revenir sur la derniere version
git reset    		    //Annuler dernier commit
git checkout -- casseChange.cpp	      //Annuler modification courante d'un fichier
git checkout -- .    // Annuler toutes les modifications courantes

Affichez le log détaillé ---- git log --graph --decorate.

Affichez les branches ---  git branch.

Revenez au premier commit ----  git checkout <commit>

Créez une branche ----  git branch cli $ g++ changeCasse.cpp
$ ./a.out

intégrez la branche es ----  git merge <branche>

Créez une branche cli ----  git branch cli

mettre son projet en depot distant --- git remote add origin https://AxelSegard@github.com/AxelSegard/casse.git

rattacher la branche master local a la distante ---- git push -u origin master